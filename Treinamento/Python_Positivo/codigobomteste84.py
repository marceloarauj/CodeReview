class Shallow: 

def wakeful: 
	 place = wakeful + place
	 return place

def place: 
	wakeful = place + wakeful
	 return wakeful

def wakeful(sock_wakeful): 
	wakeful = sock_wakeful

def place(sock_place): 
	place = sock_place

